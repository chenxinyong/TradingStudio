# TODO — 本周待办 (9/21 周一 → 9/27 周日)

> 当前: main 分支
> 主线: **Carry 反转因子 C# 接入**（补「无正期望策略」短板）+ **Live 顺手验证**（state-model 修复实盘确认）

---

## ✅ 已完成（9/19 收口，提交 8724876）

ChatGPT 审计（`docs/trading/Clippings/运行日志复核.md`）发现的 P0 结构性债已全部落地，测试 335→340 全绿：

- ✅ CTP 事实入领域模型：`OrderEvent` 补 `TradeId/OrderSysId/ExchangeId/OffsetFlag/TradingDay`，`_orderStrategy` 映射回填 StrategyId
- ✅ 成交幂等：`TradeDeduplicator` + FillChannel 改 Unbounded + `TryWriteEvent` 查返回值 + `DroppedEvents` 计数
- ✅ 持仓对账：`ReconcileStatus` + Mismatch 时阻断新开仓（block 侧，平仓放行）
- ✅ CanTrade 四态状态机：`IsLoggedIn / SettlementConfirmed / PositionReconciled / AccountReconciled`
- ✅ 领域建模/并发/日志：今昨仓拆分、PositionSnapshot、BrokerAccountSnapshot、SubPortfolio 加锁、结构化审计日志

**唯一缺口**：周六深夜提交、周末休市，**从未在实盘验证过**——这是本周「Live 顺手验证」的由来。

---

## 🔴 主攻：Carry 反转因子 C# 接入

> 目标：把研究侧已验证（Sharpe 1.44 / OOS 1.60）的 Carry 反转因子接进 C#，补上「正期望策略」这块最长短板。

### Carry 三口径（文档口径，开工前定死）

| # | 分歧 | 采用 | 弃用 |
|---|------|------|------|
| 1 | 分母 | `(近月−次月)/次月` | roll_yield 的 `/近月` |
| 2 | 多空 | **做多低 Carry（深升水）、做空高 Carry（深贴水）＝ Q1−Q5** | 脚本写死 Q5−Q1 |
| 3 | 近月 | `skip=1` 合约代码解析 | 到期日最早/OI 最大 |

### 步骤

- [ ] **CarryPanelBuilder** — 读 `bars_day` 单月合约（`QueryBarsAsync`，`NOT LIKE '%000'`），按「品种×交易日」取 skip=1 的两个合约，算 `(near−next)/next` → DuckDB 表 `carry_panel`（C# 内生成，不走 Python→CSV）
- [ ] **CarryEvalCommand** — 逐日截面 Rank IC（IS/OOS 切分）+ 5 分组月度多空（Q1−Q5）。**验收闸门：全样本 ≈1.44、OOS ≈1.60**，跑不出回头查三口径
- [ ] **CarryReversalStrategy** — 照抄 `CrossSectionalIntradayMomStrategy` 截面排序 + TopN/BottomN，月度调仓，long 低 carry short 高 carry，收益用 `xxx000`；配置 `configs/carry_reversal_day.json`（46 品种白名单）
- [ ] **独立回测** — 扣成本后 Sharpe ≥ 1
- [ ] **三因子组合**（拉伸）— Carry（月度）+ VWAP_Dev + IntradayMom（日内），等权起步，`CompositeFactorStrategy` 线性加权模板

**参考**：`docs/trading/因子研究/Carry反转因子深度分析.md`（定义/IC/稳健性）、`scripts/factor_research/carry_csmom_factor.py`（parse_contract + carry 口径）。

---

## 🟢 顺手：Live 验证 state-model 修复（每个交易日）

零代码、纯监控：

- [ ] 交易时段 `dotnet run -- live`（simnow），盯 `health.json`：`DroppedEvents=0`、无重连风暴
- [ ] 查 `data/bars_live.duckdb` 的 `order_events`：新字段 `TradeId/StrategyId/OffsetFlag` 真实填充、平今平昨正确
- [ ] 盯 `ReconcileStatus` NotReconciled→Ok、无 Mismatch、无拒单
- [ ] 跑满 2–3 干净交易日即封板；发现回归则优先级抬到策略之前

---

## 日节奏（本周）

| 日 | 聚焦 |
|----|------|
| **周一 9/21** | 收口（commit docs + 重写待办 + 更新记忆）+ 定 Carry 三口径 |
| **周二 9/22** | CarryPanelBuilder + 复现验证 |
| **周三 9/23** | 复现 1.44 验收闸门 + CarryReversalStrategy |
| **周四 9/24** | 独立回测（扣成本 Sharpe≥1） |
| **周五 9/25** | 三因子组合（等权起步） |
| **周六 9/26** | 周度分析（股票指数/板块个股/期货品种） |
| **周日 9/27** | 本周总结 + 下周规划 |

---

## 决策待定

1. **三因子加权**：等权（默认，过拟合少） vs IC 加权（日内 composite 里占优，但不宜直接外推到月度）
2. **组合频率冲突**：Carry 月度 vs 日内因子——MVP 先做「Carry 作慢变量叠加 + 日内因子主调仓」
