# 2026-06-14 工作计划

## 1. 检查导入进度

打开昨晚运行的 PowerShell 窗口，看最后几行输出确认当前状态。
已完成的数据已写入 `bars_history.db`，导入幂等。

## 2. 验证已导入年份

```powershell
cd c:\Works\ClaudeCode\TradingStudio\dist
dir bars_history.db
```

## 3. 继续剩余年份

根据进度，依次执行未完成的年份：

```powershell
# 2021
.\TradingStudio.exe import-jinshuyuan --layer active --from-month 202101 --to-month 202112 --db bars_history.db

# 2022
.\TradingStudio.exe import-jinshuyuan --layer active --from-month 202201 --to-month 202212 --db bars_history.db

# 2023
.\TradingStudio.exe import-jinshuyuan --layer active --from-month 202301 --to-month 202312 --db bars_history.db

# 2024
.\TradingStudio.exe import-jinshuyuan --layer active --from-month 202401 --to-month 202412 --db bars_history.db

# 2025
.\TradingStudio.exe import-jinshuyuan --layer active --from-month 202501 --to-month 202512 --db bars_history.db
```

## 4. 导入完成后

分批验证数据完整性，然后启动 **Phase 2a** 回测引擎实现：

- `TradingEngine` — 主循环
- `ExecutionHandler` — Tick 级 + Bar 级撮合
- `PortfolioManager` — 仓位资金管理
- 双均线策略端到端验证

## 状态参考

| 年份 | 月份 | 状态 |
|------|------|------|
| 2020-01 | 已导入 | ✅ |
| 2020-02 ~ 12 | 可能已跑完 | ⏳ |
| 2021 ~ 2025 | 待执行 | ⬜ |

> 导入是幂等的（`INSERT OR REPLACE` + `UNIQUE`），中断重跑安全。
> 参考：[Phase 2 回测系统设计](docs/phase2-backtest-design-v2.md)
